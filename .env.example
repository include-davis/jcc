# Base URL of the headless CMS. Must be prefixed with NEXT_PUBLIC_ so it's
# available client-side and at build/deploy time.
# Local dev example: NEXT_PUBLIC_CMS_BASE_URL=http://localhost:3001
NEXT_PUBLIC_CMS_BASE_URL=
